# ============================================================
# StadiumMind AI — Python Dependencies
# Pinned for production stability on Render
# ============================================================

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gunicorn>=21.2.0

# Data validation & settings
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# LangChain RAG pipeline
langchain>=0.1.16
langchain-community>=0.0.34
langchain-core>=0.1.46
langchain-text-splitters>=0.2.0

# Vector database
chromadb>=0.4.24
langchain-chroma>=0.1.0

# Google AI
google-genai>=0.5.0

# HTTP requests
requests>=2.31.0
httpx>=0.27.0

# Concurrency (used in assistant controller)
# (built-in concurrent.futures — no extra package needed)
